# Versions used on the measurement containers (see box/Dockerfile).
# Base image python:3.12-slim; system package espeak-ng (G2P backend of misaki).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
onnxruntime==1.20.1
openvino==2026.3.0
numpy
soundfile
psutil
kokoro>=0.9
misaki[en]>=0.9
huggingface_hub
# analysis and figures only (not needed on the measurement container):
matplotlib
# transfer check only (its own container, see box/Dockerfile.pocket):
# pocket-tts
